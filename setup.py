"""Project setup."""
#!/usr/bin/env python
# 3rd-party
from setuptools import find_packages
from setuptools import setup

setup(name='car_rating',
      version='1.0',
      packages=find_packages())
